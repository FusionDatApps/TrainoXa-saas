import {
  colors,
  radius,
  shadows,
  spacing,
  transitions,
} from "./tokens";

import { surfaces } from "./surfaces";
import { typography } from "./typography";

export const theme = {
  colors,
  spacing,
  radius,
  shadows,
  transitions,
  surfaces,
  typography,
};
